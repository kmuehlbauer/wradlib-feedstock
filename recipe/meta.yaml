{% set version = "0.11.0" %}

package:
    name: wradlib
    version: {{ version }}

source:
    fn: wradlib-{{ version }}.tar.gz
    url: https://pypi.io/packages/source/w/wradlib/wradlib-{{ version }}.tar.gz
    sha256: 1017106753146adbf318fd4b9a6d429e8a0a9c069d484709a5fc255b8b385815

build:
    number: 0
    script: python setup.py install

requirements:
    build:
        - python
        - numpy
        - scipy
        - matplotlib
        - h5py
        - netcdf4
        - gdal 2.1.*
    run:
        - python
        - numpy
        - scipy
        - matplotlib
        - h5py
        - netcdf4
        - gdal 2.1.*

test:
    imports:
        - wradlib  # [not osx]
    commands:
        # hack to get macosx matplotlib running
        # see https://github.com/conda-forge/wradlib-feedstock/pull/20
        - python -c "import matplotlib as mpl; mpl.use('Agg'); import wradlib"  # [osx]

about:
    home: http://wradlib.org/
    license: MIT
    summary: "Open Source Library for Weather Radar Data Processing"

extra:
    recipe-maintainers:
        - jjhelmus
        - kmuehlbauer
